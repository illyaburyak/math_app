import { AnswerHeighlightDirective } from './answer-heighlight.directive';

describe('AnswerHeighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerHeighlightDirective();
    expect(directive).toBeTruthy();
  });
});
